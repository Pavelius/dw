#include "main.h"

static bool character_sheet(action& a, creature* player, location& area, bool run, bool interactive) {
	return true;
}

static action standart_actions_data[] = {{NoAction, "Посмотреть листок персонажа"}
};